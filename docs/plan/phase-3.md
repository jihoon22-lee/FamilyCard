# Phase 3 — 파서 + 카드 매칭

> 버전 태그: `v0.3.0`
> **개발 진입: [ADR 0014](../adr/0014-development-and-release-gates.md) 사용자 승인에 따라 격리 DB에서 진행. Gate C0는 최종 배포 전 필수 조건.**

## 목표

쌓인 원문을 거래로 바꾸고, **카드 한 장 단위** 월간 사용금액을 보여줍니다.

## 완료 기준

- 거래성 검토 원문의 95% 이상이 자동 처리되고 나머지는 이유가 보이는 미확정 상태
- 같은 결제의 카드사 앱·결제 앱·카카오·SMS 원문이 모두 보존되면서 집계에는 한 번만 반영
- 검토 정답셋에서 알려진 오병합·오카드 매칭 0건
- 카드별 이번 달 사용금액이 보임
- 미확정 큐에서 사람이 개입 가능하고, 지정하면 `CardAlias`가 학습됨
- 규칙 수정 후 재파싱이 멱등하게 동작하고 수동 편집분은 보존됨

## 작업

### 먼저: 원문 분석

- [x] ADR 0014 개발/배포 검증 분리 승인. Gate C0와 Phase 2 배포 완료는 미확인 유지
- [x] Phase 3 schema migration 전 운영 DB 백업과 격리 복원 확인 (946건 기준 보존 검증)
- [x] 실제 값 없이 카드사×출처×문구형식 건수만 적은 커버리지 표 작성
- [ ] `/raw` 화면에서 수집된 원문을 카드사별로 분류
- [ ] 동일 결제가 카드사 앱+토스/카카오페이/네이버페이 등 여러 출처에서 온 실제 묶음 식별
- [ ] 비슷해 보여도 별도 결제인 연속 결제 음성 반례 식별
- [ ] 각 카드사의 변형을 목록화 — 승인 / 취소 / 할부 / 해외 / 마스킹 / 무시 대상
- [ ] **가공된 픽스처 작성** (실제 문자 복붙 금지 — [불변 규칙 7](../../AGENTS.md))

### 복수 출처 대사 — 파서 구현 전 필수

- [x] [ADR 0007](../adr/0007-user-managed-capture-sources.md) 기준으로 현재
      `Transaction.rawMessageId` 1:1 관계를 재검토하고 마이그레이션 설계
- [x] 한 거래에 복수 `RawMessage` 근거를 연결하되 원문은 전부 영구 보존 (ADR 0015, 격리 migration 검증)
- [x] 같은 구성원·카드·금액·거래종류·승인시각·가맹점 기반 보수적 후보 점수화
- [x] 자동 확정 기준 미달·동점 후보는 합치지 않고 사람 확인 대상으로 표시
- [x] 대표 원문 키를 유지하고 모든 출처 근거 보존 (ADR 0015); 근거 부족 시 자동 권위 선정하지 않음
- [x] 재파싱 뒤에도 같은 의미 거래가 집계에 정확히 한 번만 기여

### 파서

- [x] [단계별 계획 S04](staged-execution-plan.md)의 지속 처리 작업·진행/실패 이유·규칙 버전 구현
- [x] RCS JSON 안내문 추출과 원본 보존 분리, 버튼 문구의 거래 오인 방지
- [x] `src/lib/parser/` — `ParserRule` priority 순 적용, 첫 매치 채택
- [x] `fieldMap` 변환기 — `money` · `installment` · `datetime_md` · `text` · `card_token` · `const`
- [x] **연도 없는 날짜 처리** — `receivedAt` 기준, 미래면 -1년
- [ ] `IGNORED` 판정 규칙 (광고 · 명세서 안내 · 결제일 안내)
- [x] 초기 표본에 없던 문구의 규칙 부재/필드 오류를 구분하고 미확정 이유·건수 표시
- [x] 규칙 부재를 무시 대상으로 삼거나 카드/금액을 추측해 집계하지 않음
- [x] `cardToken`은 **정규화하지 않고 원문 그대로** 넘김

### 카드 매칭

- [x] `src/lib/cardmatch/` — 5단계
  1. `CardAlias` 조회
  2. 정확 일치 `(memberId, issuer, last4)`
  3. 마스킹 패턴 (`1*34` → `/^1.34$/`) — **1장일 때만 확정**
  4. `nickname` 부분일치 — 1장일 때만
  5. 실패 → `NEEDS_CARD`
- [x] 후보 조회에 **`memberId` 조건 필수** (타인 카드가 후보에 들어가면 안 됨)
- [x] 사람이 지정 시 `CardAlias` 자동 생성

### 취소

- [x] 원거래 후보 조회 (같은 카드, 60일 이내, 잔여액 ≥ 취소액)
- [x] 점수 매기기 (가맹점 일치, 금액 일치, 최근성) + 2위와의 격차 검사
- [x] `canceledAmount` 누적 — **하나의 DB 트랜잭션으로**
- [x] 고아 취소 → `isOrphanCancellation`
- [x] **재파싱 시 이중 차감 방지** — `canceledAmount` 초기화 후 전체 재적용

### 미확정 큐 UI

- [x] 파싱 실패 큐
  - 원문 표시 + 수동 입력 (`isManuallyEdited = true`)
  - **"이 문구로 규칙 만들기"** — 정규식 초안 생성
  - 저장 전 같은 issuer의 다른 실패 건에 시험 적용해 몇 건이 풀리는지 미리보기
- [x] 카드 미확정 큐
  - 카드 선택 → `CardAlias` 생성
  - "새 카드 등록" 경로

### 재파싱

- [x] `POST /api/reparse` — scope(ALL / ISSUER / STATUS / DATE_RANGE)
- [x] `isManuallyEdited` 건 건너뛰기
- [x] 파싱 실패 시 기존 `Transaction` **유지** (삭제 금지)
- [x] `dryRun` — `wouldFix` / `wouldBreak` 미리보기
- [x] `wouldBreak > 0`이면 UI에서 경고

### 화면

- [x] 대시보드 — 이번 달 **카드별 사용금액 리스트**
- [x] 카드 탭 → 거래 내역
- [x] 미확정 건수 배지 (상단 상시 노출)
- [x] 카드 관리 — 등록 · 수정 · 비활성화
- [x] 파서 규칙 관리 화면

### 테스트

- [x] 같은 결제의 `CARD_APP` + `PAYMENT_APP` 원문 → 원문 2건 유지, 거래/집계 1건 ★
- [ ] 같은 금액·가맹점의 실제 별도 연속 결제 2건 → 잘못 합치지 않음 ★
- [x] 복수 출처 후보가 애매하면 자동 병합하지 않음
- [ ] 카드사별 승인 / 취소 / 할부 / 해외 파싱
- [x] 연말연시 날짜 (12/31 결제가 1월에 업로드)
- [x] 카드 매칭 — 정확일치 / 마스킹 단일후보 / **마스킹 2장 → `NEEDS_CARD`** / 별칭 / alias 학습
- [x] **타인 카드가 후보에 포함되지 않음**
- [x] 취소 — 전액 / 부분 2회 누적 / 잔여액 초과 실격 / 고아
- [x] **재파싱 2회 → `canceledAmount`가 두 배 안 됨** ★
- [x] 재파싱 시 수동 편집분 보존
- [ ] 새로운 가공 형식 도착 → 미확정 → 규칙 추가 → 과거/신규 처리 → 두 번 재처리해도
      거래 중복·취소 이중 차감 없이 수동 수정과 원문 보존 ★

## 설계 문서 참조

- [수집 이후 통합 실행 순서](post-collection-execution.md)
- [03-parser](../design/03-parser.md)
- [04-card-matching](../design/04-card-matching.md)
- [05-cancellation](../design/05-cancellation.md)

## 주의

### 추측해서 확정하지 않는다

마스킹 매칭에서 후보가 2장이면 **아무거나 고르지 않습니다.** 엉뚱한 카드에 거래가 붙으면 두 카드의 실적이 모두 틀어지고, 사용자는 왜 틀렸는지 알 수 없습니다.

미확정 큐에 한 번 뜨는 비용이 훨씬 쌉니다.

### 재파싱 이중 차감

★ 표시한 항목은 실제로 놓치기 쉬운 버그입니다. 재파싱은 이 시스템의 핵심 기능이라 자주 돌게 되는데, 그때마다 취소가 중복 반영되면 실적이 계속 줄어듭니다. **반드시 테스트로 고정하세요.**

### 픽스처에 실제 문자를 넣지 않는다

원문을 보면서 규칙을 쓰다 보면 그대로 복사해 테스트에 붙이기 쉽습니다. 가맹점명·금액·이름·카드번호를 전부 가짜로 바꾸되, **구두점과 공백 구조는 실물 그대로** 두세요.

## 다음

[Phase 4 — 실적 엔진](phase-4.md)

## S03 모델 검증

- [x] 규칙 action/version/revision, 처리 작업 lease/세대, 검토 판단 모델
- [x] 외화 scale/원화 미확정, 시각 정밀도/원결제일, 카드·별칭 유효 기간
- [x] 수동/명세서 원문 소유자와 원본 파일 모델, 원문 scope 회귀
- [x] 격리 복원 migration/schema diff/946건 원문 보존, 가공 legacy backfill

## 순수 엔진 (DB 처리와 UI 연결은 다음 단계)

- [x] 카드 후보 구성원/기간/별칭·마스킹 충돌 방어
- [x] 고유 사건 근거 기반 중복 판정과 모호한 후보 REVIEW
- [x] 취소 원점 재계산·부분취소 누적·고아 재평가·수동 연결 우선·당일 DAY 정밀도
- [x] 가공 승인/부분취소/외화/연말연시/윤년/동점/재처리 멱등 회귀 테스트

## 지속 처리 연결

- [x] ingest 원문/작업 원자적 생성·누락 작업 복구·작은 배치
- [x] lease 만료 복구·세대 교체 롤백·실패 재시도·SERIALIZABLE 쓰기
- [x] 실제 DB의 복수 근거→한 거래·부분취소·반복 재처리·수동/기존 거래 보존
- [x] standalone 후보 HTTP ingest 이후 자동 파싱/카드 연결 (격리 DB)
- [x] 지속 재처리 요청·dry-run·규칙/검토 UI와 연결

## 카드·검토·월 사용액 구현 검증

- [x] 카드/별칭 관리와 기간·비활성화, 본인/타인 접근 회귀
- [x] 수동 거래/카드 학습/취소 원거래 지정/병합·분리와 감사 이력
- [x] KST 월별 순사용액·확인 필요/원화 미확정 분리, 원문 보존 통합 검증
- [ ] 실제 카드/규칙 등록 후 검토 정답셋 대조 (최종 검증)

- [x] 관리자 규칙 버전/샘플/최근 실패 시험/복원과 동시 변경 방어 (가공 DB 검증)

## 최종 개발 검증 보강

- [x] 실제 원문 값 없이 955건의 출처/키워드 집계 작성 ([인벤토리](../research/raw-format-inventory-2026-09-25.md))
- [x] RCS legacy LinearLayout/TextView의 취소 본문 추출, 버튼/링크 배제와 구조 상한
- [x] 실제 부분취소 1건을 값 출력/규칙 등록 없이 메모리에서 파싱 확인. 가공 픽스처는 실제 공백 구조 유지
- [ ] 실제 카드 등록/규칙 검토·별도 결제 정답셋·자동 처리율 95% 대조 (구현 완료와 구분)

- [x] 리뷰 R01: 취소 후보 연결 성분 증분 투영, 3,000회 동등성·12,000건 카드 처리·관리자 복구 도구 (#44).

- [x] 리뷰 R04: 재처리 P2034 재시도·20주기 상한·격리 DB 회귀 (#46).

- [x] 리뷰 R05: 대표 원문 분리 안내와 근거 3개 배치·취소·원문 보존 회귀 (#47).

- [x] R01 추가 성능 보강: 수동 고정 연결 직접 조회·5,500건 원장 비교 (#49).
